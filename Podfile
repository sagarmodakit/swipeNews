# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'swipeNews' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swipeNews
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'SDWebImage', '~> 4.0'
  pod 'RxSwift'
  pod 'SVProgressHUD'
  pod 'RxCocoa'

  target 'swipeNewsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swipeNewsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
